How to run the Win_flex :-

FOLLOW THIS COMMANDS :-

    1 :- win_flex -o <created file.c>(need to created by win_flex) <created file.l>

        here after -o means we are telling that compiler to create a " .c " file with respect to " .l " file

    2 :- gcc <created c file > -o <created file.exe>

        here in above step compiler create a c file now we need to compiler that " c " file to " .exe " file
    
    3 :- .\created file.exe
